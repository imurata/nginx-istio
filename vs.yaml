piVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: nginx-vs
spec:
  gateways:
  - nginx-gateway
  hosts:
  - 'nginx.10.191.253.245.sslip.io'
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: nginx
        port:
          number: 80
